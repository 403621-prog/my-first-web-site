# Replit setup

This project is a dependency-free static HTML/CSS site.

## Run

Use the Replit **Run** button. It starts the `Start application` workflow, which serves the project files on port 5000 with Python's built-in HTTP server.